// High-Fidelity Network Configuration for Elite School Portal
const getApiUrl = () => {
  // Use relative paths for development (Vite Proxy) and production
  return '';
};

export const API_URL = getApiUrl();
